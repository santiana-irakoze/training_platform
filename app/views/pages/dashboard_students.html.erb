<div class="dashboard-container">
  <div><%= render "tabs" %></div>

  <!-- Turbo Frame Container -->
  <%= turbo_frame_tag "main-content" do %>
    <%= render "pages/preview", tests: @tests %> <!-- Default Content -->
  <% end %>
</div>
